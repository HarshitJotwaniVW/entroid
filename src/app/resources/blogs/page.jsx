import { BlogList } from '../../../components/BlogList'

export const metadata = {
  title: 'Blog · Entroid',
  description: 'Real problems from large enterprises, and the operating-layer pattern behind them.',
}

export default function BlogsPage() {
  return <BlogList />
}
